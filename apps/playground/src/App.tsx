import React from 'react';
// @ts-ignore
import { Button } from '@support-library/ui/';

export default function App() {
  return (
    <div style={{display: 'flex', flexDirection: 'column'}}>
      <h1>Playground</h1>
      <Button>Testando</Button>
    </div>
  );
}